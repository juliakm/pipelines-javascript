trigger: 
- master

pool: windows-latest

steps:
- script: |
   echo "##vso[task.setvariable variable=first;]foo"
   displayName: foo
   continueOnError: true
- script: |
   echo "##vso[task.setvariable variable=second;]foo:bar"
   displayName: foo:bar
   continueOnError: true
- script: |
   echo "##vso[task.setvariable variable=third;]foo%3abar"
   displayName: foo%3bar
   continueOnError: true
- script: |
    echo $FIRST
    echo $SECOND
    echo $THIRD


# variables:
# - name: myJob
#   value: MyJob


# trigger:
# - master

# pool:
#   vmImage: 'ubuntu-latest'

# steps:
# - task: NodeTool@0
#   inputs:
#     versionSpec: '10.x'
#   displayName: 'Install Node.js'

# - script: |
#     printenv
#     npm install
#     npm run build
#   displayName: 'npm install and build'

# - task: CopyFiles@2
#   inputs:
#     Contents: '$(Build.Repository.LocalPath)/**' # Pull the build directory (React)
#     TargetFolder: '$(Build.ArtifactStagingDirectory)'
