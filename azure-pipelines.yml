parameters:
- name: myFirstObject
  type: object
  default: default
  values:
   - default
   - ubuntu
   - windows
- name: mySecondObject
  type: object
  default:
    foo: FOO
    bar: BAR

variables:
   A: ${{ join(';',parameters.myFirstObject) }}

 # A: $[join(';','[foo, bar]')]
 # B: $[upper('bah')]
  #C: $[join(';','${{ parameters.myArray }}']

steps:
  - script: |
      echo $A
    #  echo $B