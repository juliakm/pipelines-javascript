parameters:
- name: myArray
  type: object
  default: default
  values:
   - default
   - ubuntu
   - windows

variables:
   A: ${{ join(';',parameters.myArray) }}

 # A: $[join(';','[foo, bar]')]
 # B: $[upper('bah')]
  #C: $[join(';','${{ parameters.myArray }}']

steps:
  - script: |
      echo $A
    #  echo $B